--extra-index-url https://download.pytorch.org/whl/cu126

# Core GUI & Utilities
customtkinter==6.0.0
PySide6==6.11.2
Pillow==12.3.0
pystray==0.19.5
numpy==2.5.3

# Audio & Local AI (Whisper with CUDA support)
torch==2.14.0
openai-whisper==20250625

# Sound-event classifier (YAMNet, laughter/screaming). The CUDA 13 build with its own CUDA/cuDNN wheels; it runs in a
# separate process because torch above ships CUDA 12 with identically named DLLs (see src/core/sound_classifier.py)
onnxruntime-gpu[cuda,cudnn]==1.30.0

# Cloud LLM APIs
openai==3.16.2
anthropic==1.7.0
google-genai==2.24.0
